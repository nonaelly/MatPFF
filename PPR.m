% PPR cohesive model
d_t
d_n
psi = min(phi_n, phi_t) + 

%% Recognition process
% step1 Δn <= δn
if d_n > D_n || (d_n <= D_n && abs(d_t) > D_t)
% interface failure
else
    % normal state judge

    % tangent state judge
end